---
name: etf-bollinger-backtest
description: "布林带(Bollinger Bands)突破策略回测与ETF技术信号生成。用baostock拉取A股/指数日线数据，计算MA20±2σ突破信号，回测胜率/年化/假突破率，输出结构化报告。"
version: 1.0.0
tags: [investment, backtest, bollinger, etf, technical-analysis]
---

# ETF 布林带突破策略回测

用统计学方法（布林带）过滤市场噪音，识别高置信度突破信号，并通过历史回测验证策略有效性。

## 核心策略

**入场**：连续 N 日收盘价突破 MA20 + 2σ 上轨 → 确认上涨趋势

**出场**：收盘价跌回上轨内 + MA20 走平（5日斜率 < 阈值）

**原理**：正态分布下，价格落在 MA±2σ 内的概率约 95.4%。突破 2σ 属于统计意义上的极端事件，可能是趋势信号。

> ⚠️ **重要提示**：股价收益率服从尖峰肥尾分布（leptokurtic），非严格正态。实际假突破率通常远高于理论 4.6%。回测验证是必经步骤，不可盲信理论。

## 快速开始

### 依赖安装

```bash
pip install baostock pandas numpy openpyxl
```

### 运行回测

```bash
# 默认：沪深300指数，近3年
python scripts/bollinger_backtest.py

# 自定义标的和参数
python scripts/bollinger_backtest.py \
  --codes sh.600519 sh.000300 sz.000001 \
  --start 2023-01-01 \
  --end 2026-06-01 \
  --window 20 \
  --std 2 \
  --confirm 2 \
  --output report.json
```

### 生成 ETF 技术信号

```bash
# 单只 ETF 信号分析
python scripts/etf_signal.py --code 510300

# 批量分析
python scripts/etf_signal.py --codes 510300 159570 512480
```

## 脚本说明

| 脚本 | 功能 |
|------|------|
| `scripts/bollinger_backtest.py` | 布林带突破策略历史回测，输出胜率/年化/假突破率/逐笔交易 |
| `scripts/etf_signal.py` | 实时 ETF 技术信号生成，计算布林带/MACD/RSI/均线系统 |

## 回测输出指标

| 指标 | 含义 |
|------|------|
| 胜率 | 盈利交易占总交易的比例 |
| 年化收益 | 策略的复合年化回报率 |
| 买入持有年化 | 同期买入并持有的年化回报率（基准） |
| 超额年化 | 策略年化 - 基准年化 |
| 假突破率 | 入场后峰值盈利 < 2% 的交易占比 |
| 平均持仓天数 | 每笔交易的平均持有时间 |

## baostock 代码格式

| 市场 | 格式 | 示例 |
|------|------|------|
| 上证 | `sh.代码` | `sh.600519`（贵州茅台）、`sh.000300`（沪深300指数） |
| 深证 | `sz.代码` | `sz.000001`（平安银行）、`sz.399006`（创业板指） |

> ETF 在 baostock 中数据可能不全（如 510300 仅 96 条），建议用指数代码（000300）代替 ETF 代码（510300）做回测。

## 已知坑点

### baostock 数据范围
- 默认返回上市以来全部数据，需用 `start_date`/`end_date` 限制
- 复权类型 `adjustflag="2"` = 前复权（推荐回测用）
- 指数（000300/399006 等）数据齐全，ETF 可能缺数据

### 正态分布假设
- 股价收益率是肥尾分布，2σ 突破出现频率远高于理论 4.6%
- 实测假突破率 55-67%（3 年回测），文章声称的 "5%极端信号=真趋势" 不成立
- 策略有效性高度依赖标的和市场环境，不存在 "一招鲜"

### 出场时机
- "跌回上轨+均线走平" 的出场逻辑偏晚，利润回吐明显
- 可考虑加入移动止盈（如从最高点回撤 3% 出场）优化

### A 股做空限制
- 回测中 "跌破下轨做空" 在 A 股实际不可行（融券门槛高、券源少）
- 策略实际只能做多或空仓，不出信号时等同于空仓

## 扩展方向

1. **参数优化**：遍历 window(10-60) × std(1.5-3.0) × confirm(1-3) 寻找最优组合
2. **移动止盈**：替代 "跌回上轨" 出场，减少利润回吐
3. **成交量确认**：突破时要求放量（如成交量 > 20日均量 1.5 倍）
4. **多标的组合**：跨行业 ETF 组合回测，降低单标的风险
5. **样本外验证**：用 2023-2025 训练，2026 数据做 out-of-sample 验证
